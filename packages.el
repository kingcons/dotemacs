(message "sup")

;; TODO:
;;; Define an error type for a missing package
;;; Define a helper to determine if a guix package is installed
;;; Define a helper to error if package is missing or load file
;;; Define a map of guix package names -> init-file names
;;; Loop over the map, calling helper on each entry

;; Initial package list: sly, magit, avy. Maybe notmuch or org?
;; Probably some others I'm forgetting. Check the browser tabs :)

